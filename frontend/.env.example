# Frontend environment. Variable NAMES only — never commit a value.
#
# Base URL of the Employee Portal backend. `services/api.js` appends /api/v1,
# and `services/careers.js` appends /api/v1/hrms/careers.
# Development: http://localhost:5001
VITE_API_URL=
